extends layout

block content
    .ui.teal.menu
        a.item(href='/')
            i.photo.icon
            span Galeria
        a.item(href='/admin/')
            i.settings.icon
            span Administração
    .ui.error.message
        h1.ui.header= message
        h2.ui.header= error.status
        pre #{error.stack}
